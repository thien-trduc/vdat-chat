import { GenerateColorPipe } from './generate-color.pipe';

describe('GenerateColorPipe', () => {
  it('create an instance', () => {
    const pipe = new GenerateColorPipe();
    expect(pipe).toBeTruthy();
  });
});
