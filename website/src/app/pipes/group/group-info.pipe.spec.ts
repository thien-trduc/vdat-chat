import { GroupInfoPipe } from './group-info.pipe';

describe('GroupInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
