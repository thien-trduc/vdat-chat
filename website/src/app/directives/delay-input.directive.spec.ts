import { DelayInputDirective } from './delay-input.directive';

describe('DelayInputDirective', () => {
  it('should create an instance', () => {
    const directive = new DelayInputDirective();
    expect(directive).toBeTruthy();
  });
});
